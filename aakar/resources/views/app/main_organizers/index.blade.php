@extends('layouts.app')

@section('content')
<div class="container">
    <div class="searchbar mt-0 mb-4">
        <div class="row">
            <div class="col-md-6">
                <form>
                    <div class="input-group">
                        <input
                            id="indexSearch"
                            type="text"
                            name="search"
                            placeholder="{{ __('crud.common.search') }}"
                            value="{{ $search ?? '' }}"
                            class="form-control"
                            autocomplete="off"
                        />
                        <div class="input-group-append">
                            <button type="submit" class="btn btn-primary">
                                <i class="icon ion-md-search"></i>
                            </button>
                        </div>
                    </div>
                </form>
            </div>
            <div class="col-md-6 text-right">
                @can('create', App\Models\MainOrganizer::class)
                <a
                    href="{{ route('main-organizers.create') }}"
                    class="btn btn-primary"
                >
                    <i class="icon ion-md-add"></i> @lang('crud.common.create')
                </a>
                @endcan
            </div>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <div style="display: flex; justify-content: space-between;">
                <h4 class="card-title">
                    @lang('crud.main_organizers.index_title')
                </h4>
            </div>

            <div class="table-responsive">
                <table class="table table-borderless table-hover">
                    <thead>
                        <tr>
                            <th class="text-left">
                                @lang('crud.main_organizers.inputs.img')
                            </th>
                            <th class="text-left">
                                @lang('crud.main_organizers.inputs.name')
                            </th>
                            <th class="text-left">
                                @lang('crud.main_organizers.inputs.position')
                            </th>
                            <th class="text-left">
                                @lang('crud.main_organizers.inputs.phone')
                            </th>
                            <th class="text-left">
                                @lang('crud.main_organizers.inputs.email')
                            </th>
                            <th class="text-center">
                                @lang('crud.common.actions')
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @forelse($mainOrganizers as $mainOrganizer)
                        <tr>
                            <td>
                                <x-partials.thumbnail
                                    src="{{ $mainOrganizer->img ? \Storage::url($mainOrganizer->img) : '' }}"
                                />
                            </td>
                            <td>{{ $mainOrganizer->name ?? '-' }}</td>
                            <td>{{ $mainOrganizer->position ?? '-' }}</td>
                            <td>{{ $mainOrganizer->phone ?? '-' }}</td>
                            <td>{{ $mainOrganizer->email ?? '-' }}</td>
                            <td class="text-center" style="width: 134px;">
                                <div
                                    role="group"
                                    aria-label="Row Actions"
                                    class="btn-group"
                                >
                                    @can('update', $mainOrganizer)
                                    <a
                                        href="{{ route('main-organizers.edit', $mainOrganizer) }}"
                                    >
                                        <button
                                            type="button"
                                            class="btn btn-light"
                                        >
                                            <i class="icon ion-md-create"></i>
                                        </button>
                                    </a>
                                    @endcan @can('view', $mainOrganizer)
                                    <a
                                        href="{{ route('main-organizers.show', $mainOrganizer) }}"
                                    >
                                        <button
                                            type="button"
                                            class="btn btn-light"
                                        >
                                            <i class="icon ion-md-eye"></i>
                                        </button>
                                    </a>
                                    @endcan @can('delete', $mainOrganizer)
                                    <form
                                        action="{{ route('main-organizers.destroy', $mainOrganizer) }}"
                                        method="POST"
                                        onsubmit="return confirm('{{ __('crud.common.are_you_sure') }}')"
                                    >
                                        @csrf @method('DELETE')
                                        <button
                                            type="submit"
                                            class="btn btn-light text-danger"
                                        >
                                            <i class="icon ion-md-trash"></i>
                                        </button>
                                    </form>
                                    @endcan
                                </div>
                            </td>
                        </tr>
                        @empty
                        <tr>
                            <td colspan="6">
                                @lang('crud.common.no_items_found')
                            </td>
                        </tr>
                        @endforelse
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="6">
                                {!! $mainOrganizers->render() !!}
                            </td>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>
@endsection
